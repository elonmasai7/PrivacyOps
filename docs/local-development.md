# Local Development Guide

1. Start PostgreSQL and Redis (Docker recommended).
2. Configure environment variables from `.env.example` files.
3. Run backend at `localhost:8000`.
4. Run frontend at `localhost:3000`.
5. Register a real account and create a real organization to begin workflows.
